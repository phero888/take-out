<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <insert id="insertBatch">
        insert into setmeal_dish (setmeal_id,dish_id, name, price,copies)
        values
        <foreach collection="setmealDishes" item="sd" separator=",">
            (#{sd.setmealId},#{sd.dishId}, #{sd.name}, #{sd.price},#{sd.copies})
        </foreach>
    </insert>

    <select id="getSetMealByIdBatch" resultType="java.lang.Long">
        select distinct setmeal_id from setmeal_dish
        <where>
            dish_id IN
            <foreach collection="ids" item="dishId" open="(" close=")" separator=",">
                #{dishId}
            </foreach>
        </where>
    </select>

    <delete id="deleteBySetmealIdBatch">
        delete from setmeal_dish
        <where>
            setmeal_id IN
            <foreach collection="ids" item="setmeal_id" open="(" close=")" separator=",">
                #{setmeal_id}
            </foreach>
        </where>
    </delete>

    <update id="update" parameterType="com.sky.dto.DishDTO">
        update dish
        <set>

            <if test="description != null">
                description = #{description},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        <where>
            id = #{id}
        </where>
    </update>
</mapper>
