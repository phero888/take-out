<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into orders(number, user_id, address_book_id, order_time, pay_status, amount, remark, phone,consignee,
        estimated_delivery_time,delivery_status,tableware_number, tableware_status, pack_amount)
        values(#{number}, #{userId}, #{addressBookId}, #{orderTime}, #{payStatus}, #{amount}, #{remark}, #{phone},#{consignee},
        #{estimatedDeliveryTime},#{deliveryStatus},#{tablewareNumber}, #{tablewareStatus}, #{packAmount})
    </insert>
</mapper>
